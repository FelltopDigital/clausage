{
  "name": "clausage",
  "version": "0.1.0",
  "type": "module",
  "description": "Sync your local Claude Code usage to clausage.com — a shareable activity grid for your bio.",
  "license": "MIT",
  "bin": {
    "clausage": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "prepublishOnly": "tsup"
  },
  "keywords": ["claude", "claude-code", "usage", "cli", "stats"],
  "dependencies": {
    "commander": "^13.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@clausage/shared": "workspace:*",
    "@types/node": "^22.10.7",
    "tsup": "^8.3.5",
    "vitest": "^3.0.5"
  }
}
